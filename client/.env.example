# Local development — Vite dev-server proxy forwards /api to http://localhost:5000
# (see client/vite.config.js)
VITE_API_URL=/api

# Production (Vercel) — set in the Vercel dashboard, NOT in a committed .env file:
# VITE_API_URL=https://<your-render-service>.onrender.com/api
